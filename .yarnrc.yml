# Yarn Berry (v2+) defaults to the Plug'n'Play linker, which Expo, Metro, and
# React Native native modules (e.g. unrs-resolver) do not support and fail to
# build under. Force the classic node_modules layout so `yarn install` works.
nodeLinker: node-modules
